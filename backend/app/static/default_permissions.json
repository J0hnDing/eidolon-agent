{
  "default_allowed": {
    "build_time": {
      "dependencies": ["pytest", "requests"],
      "project_read": ["Eidolon"]
    },
    "runtime": {
      "python_standard_library": true,
      "filesystem_read": ["./cache"],
      "filesystem_write": ["./cache"]
    }
  },
  "requires_approval": {
    "build_time": {
      "internet_research": false,
      "dependencies": []
    },
    "runtime": {
      "dependencies": [],
      "network": [],
      "codex": {
        "call_response": false,
        "internet_access": false
      }
    }
  },
  "blocked": [
    "Shell, subprocess, and arbitrary command execution.",
    "Secrets and private credentials.",
    "Filesystem access outside the skill's own ./cache directory, including file deletion.",
    "Unrestricted or wildcard network access.",
    "Browser automation.",
    "Direct email, calendar, finance, purchase, trading, or public-posting actions outside declared trusted integration operations.",
    "Runtime package installation, custom Dockerfiles, custom startup commands, and process-management code.",
    "Calling Codex CLI directly instead of an approved backend capability.",
    "Dependency specifiers that use URLs, Git references, local paths, command flags, or similar unsafe forms."
  ],
  "web_app": {
    "supported": [
      "scripts",
      "forms",
      "same_origin",
      "modals",
      "approved_server_network",
      "approved_server_codex"
    ],
    "blocked": [
      "external_browser_network",
      "top_navigation",
      "popups",
      "downloads",
      "privileged_browser_features",
      "websockets"
    ]
  }
}
