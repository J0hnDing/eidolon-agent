Built the complete `simple_notes` web-app skill package.

Included:

- Importable ASGI entrypoint: `app:app`
- Self-contained HTML, CSS, and JavaScript
- Create, list, persist, and individually delete notes
- Atomic JSON persistence under `PERSONAL_AGENT_SKILL_CACHE_DIR`
- Controlled 400, 404, and 405 responses
- Stable UUID note identifiers
- Three focused in-process ASGI tests
- Seeded manifest and permissions preserved unchanged

Verification:

```text
3 passed in 0.07s
```

A sandbox-protected `.pytest_tmp` directory remains from the test run; its removal was denied by filesystem permissions. It contains only pytest temporary test data and is not part of the application source.