You are TesterAgent validating one DAG task node or the final end-to-end behavior of an application skill.

Read the ProductManager blueprint, current task node, task acceptance criteria, parent interface artifacts, and Builder-created files. Then write rich but not overly complicated pytest tests for the current task node or final E2E validation.

Rules:
- Write only the test file named in Tester context, such as tests/test_<task_id>.py or tests/test_final_e2e.py.
- Do not edit manifest.json, README.md, SKILL.md, skill.py, cache files, app source code, project metadata, or git files.
- Use only Python standard library and pytest.
- Prefer 3 to 6 focused tests.
- Test the manifest contract, representative successful input, JSON stdin/stdout behavior for executable skills, and one or two important edge cases from the blueprint.
- Include checks that correspond to the current task node acceptance criteria or final DAG expectations.
- If interface_type is tool, verify manifest.json has a declarative tool_ui_schema with fields.
- Do not require network, secrets, shell commands, package installation, browser automation, email/calendar/finance actions, public posting, purchases, trading, or file deletion.
- Use subprocess inside pytest only for executable JSON stdin/stdout checks.

Return no prose. Write files only.

Controlled skill folder:
C:\Users\John\personal-agent\skills\proposed\wordle_game

Skill:
- name: wordle_game
- skill_type: automation
- interface_type: tool

Tester context:
{
  "skill_id": 2,
  "action": "tester_final_e2e",
  "original_user_request": "build a wordle game",
  "intent_prompt": {
    "schema_version": 1,
    "original_user_request": "build a wordle game",
    "refined_prompt": "build a wordle game",
    "selected_memory_facts": []
  },
  "blueprint_json": {
    "goal": "Create a local Wordle-style game skill that lets the user start or continue a six-attempt five-letter word guessing game, submit guesses, and receive structured feedback without network access.",
    "skill_name": "wordle_game",
    "skill_type": "automation",
    "interface_type": "tool",
    "expected_files": [
      "manifest.json",
      "README.md",
      "skill.py"
    ],
    "expected_behavior": {
      "gameplay": "The skill maintains one local cached game state, accepts a five-letter alphabetic guess, compares it to the current answer, returns per-letter feedback, tracks guess history, and reports whether the game is in progress, won, lost, reset, or invalid.",
      "new_game": "When new_game is true, the skill clears the current cached game state, starts a fresh game, then applies the submitted guess.",
      "input_schema": {
        "type": "object",
        "properties": {
          "guess": {
            "type": "string",
            "description": "A five-letter alphabetic Wordle guess.",
            "minLength": 5,
            "maxLength": 5,
            "pattern": "^[A-Za-z]{5}$"
          },
          "new_game": {
            "type": "boolean",
            "description": "Start a fresh game and clear the current cached game state.",
            "default": false
          }
        },
        "required": [
          "guess"
        ],
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "in_progress",
              "won",
              "lost",
              "reset",
              "error"
            ]
          },
          "result": {
            "type": "string"
          },
          "attempts_used": {
            "type": "integer",
            "minimum": 0,
            "maximum": 6
          },
          "attempts_remaining": {
            "type": "integer",
            "minimum": 0,
            "maximum": 6
          },
          "feedback": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "letter": {
                  "type": "string"
                },
                "state": {
                  "type": "string",
                  "enum": [
                    "correct",
                    "present",
                    "absent"
                  ]
                }
              },
              "required": [
                "letter",
                "state"
              ],
              "additionalProperties": false
            }
          },
          "history": {
            "type": "array",
            "items": {
              "type": "object"
            }
          }
        },
        "required": [
          "status",
          "result",
          "attempts_used",
          "attempts_remaining",
          "feedback",
          "history"
        ],
        "additionalProperties": false
      },
      "tool_ui_schema": {
        "title": "Wordle Game",
        "description": "Play a local six-attempt five-letter word guessing game.",
        "submit_label": "Submit Guess",
        "fields": [
          {
            "name": "guess",
            "label": "Guess",
            "type": "text",
            "placeholder": "CRANE",
            "help_text": "Enter exactly five letters.",
            "required": true,
            "default": ""
          },
          {
            "name": "new_game",
            "label": "Start New Game",
            "type": "checkbox",
            "placeholder": "",
            "help_text": "Reset the current saved Wordle game before applying this guess.",
            "required": false,
            "default": false
          }
        ],
        "result_template": {
          "primary_field": "result",
          "primary_label": "Result"
        }
      }
    },
    "acceptance_criteria": [
      "The skill is an automation tool with a valid manifest, Python implementation, README, and install-validation tests.",
      "The tool uses only local runtime state in ./cache and requests no network, secrets, shell access, external dependencies, or broad filesystem access.",
      "The manifest declares input_schema, output_schema, runtime permissions, and declarative tool_ui_schema for the Tools UI.",
      "The tool_ui_schema defines the Wordle title, guess text field, new-game checkbox, submit label, and result rendering hint without requiring any frontend code.",
      "Guesses are validated as exactly five alphabetic characters before gameplay processing.",
      "Feedback marks each guessed letter as correct, present, or absent and preserves guess history across calls until a new game is started or the game ends.",
      "The game ends after a correct guess or after six failed attempts, returning status won or lost with attempts counts.",
      "tool_ui_schema is present so the Tools page can render a user-friendly UI"
    ],
    "product_manager_summary": "Ready to request permission to generate a low-risk local Wordle Game automation tool that writes only its own files and uses ./cache for game state.",
    "decision": "request_permission"
  },
  "permission_plan": {
    "build_time": {
      "codex_generation": true,
      "internet_research": false,
      "dependencies": [],
      "reason": "Generate the Wordle automation skill files, manifest, README, and tests from the approved blueprint without external research or package installation."
    },
    "runtime": {
      "permissions": {
        "network": [],
        "filesystem_read": [],
        "filesystem_write": [
          "./cache"
        ],
        "secrets": [],
        "shell": false
      },
      "network_domains": [],
      "dependencies": [],
      "reason": "The game runs locally, validates guesses, computes feedback in Python, and persists only the current game state and history inside the skill-owned ./cache directory. No network, secrets, shell access, external dependencies, broad filesystem reads, or broad filesystem writes are needed."
    }
  },
  "task_dag_json": {
    "schema_version": 1,
    "graph_id": "wordle_game_build",
    "root_task_ids": [
      "wordle_manifest_tool_contract"
    ],
    "nodes": [
      {
        "id": "wordle_manifest_tool_contract",
        "title": "Manifest and tool contract",
        "summary": "Create the automation skill manifest for the Wordle Game tool, including skill metadata, runtime permission declarations, input_schema, output_schema, and the declarative tool_ui_schema required by the Tools UI.",
        "depends_on": [],
        "difficulty": "medium",
        "requires_tests": true,
        "parallel_safe": false,
        "expected_inputs": [
          "blueprint.json",
          "permissions.json"
        ],
        "parent_interface_artifacts": [],
        "expected_output_paths": [
          "manifest.json"
        ],
        "file_write_claims": [
          "manifest.json"
        ],
        "acceptance_criteria": [
          "manifest.json declares skill_name wordle_game, skill_type automation, and interface_type tool.",
          "manifest.json requests no network, secrets, shell access, external dependencies, broad filesystem reads, or broad filesystem writes.",
          "manifest.json limits runtime filesystem_write permission to ./cache.",
          "input_schema requires guess, validates exactly five alphabetic characters, supports optional new_game boolean, and rejects additional properties.",
          "output_schema includes status, result, attempts_used, attempts_remaining, feedback, and history with the status and feedback enums from the blueprint.",
          "tool_ui_schema is declarative JSON only and includes title Wordle Game, guess text field, new_game checkbox, Submit Guess label, and result_template using result as the primary field.",
          "No React, HTML, JavaScript, or app frontend code is generated."
        ],
        "test_expectations": [
          "Manifest validation passes against the platform manifest schema.",
          "Tests assert tool_ui_schema is present and contains the required field names, labels, submit label, and result rendering hint.",
          "Tests assert runtime permissions match the approved permission plan exactly."
        ],
        "interface_artifact_expectations": [
          "The Tools page can render the tool from manifest.json using only tool_ui_schema.",
          "The guess field is required, text-based, labeled Guess, and includes a five-letter placeholder.",
          "The new_game field is optional, checkbox-based, labeled Start New Game, and defaults to false.",
          "The result rendering hint identifies result as the primary output field."
        ]
      },
      {
        "id": "wordle_gameplay_engine",
        "title": "Gameplay engine",
        "summary": "Implement the Python automation entry point that validates guesses, manages a single local game state in ./cache, compares guesses to the answer, returns structured feedback, tracks history, and enforces six-attempt game completion.",
        "depends_on": [
          "wordle_manifest_tool_contract"
        ],
        "difficulty": "medium",
        "requires_tests": true,
        "parallel_safe": false,
        "expected_inputs": [
          "blueprint.json",
          "permissions.json",
          "manifest.json"
        ],
        "parent_interface_artifacts": [
          "input_schema",
          "output_schema"
        ],
        "expected_output_paths": [
          "skill.py",
          "tests/test_skill.py"
        ],
        "file_write_claims": [
          "skill.py",
          "tests/test_skill.py"
        ],
        "acceptance_criteria": [
          "skill.py exposes the platform-expected callable entry point for automation skill execution.",
          "The implementation uses only Python standard library modules and does not perform network access, shell execution, secret access, package installation, or filesystem access outside ./cache.",
          "The implementation validates guess as exactly five alphabetic characters before applying gameplay logic and returns status error for invalid input.",
          "When new_game is true, the implementation clears the current cached game state, starts a fresh game, and applies the submitted guess.",
          "The skill persists one current game state and guess history in ./cache across calls until a new game starts or terminal game behavior requires reset handling.",
          "Feedback returns one object per guessed letter with uppercase letter and state correct, present, or absent.",
          "Duplicate-letter feedback is handled consistently so present markings do not overcount letters beyond the answer contents.",
          "The game returns status won after a correct guess and status lost after six failed attempts with correct attempts_used and attempts_remaining counts.",
          "Responses conform to the manifest output_schema for success, loss, win, and validation error cases."
        ],
        "test_expectations": [
          "Tests cover valid in-progress guess behavior, win behavior, six-attempt loss behavior, invalid guess rejection, new_game reset behavior, cache persistence across calls, and duplicate-letter feedback behavior.",
          "Tests use a temporary cache directory or monkeypatching so they do not write outside the test workspace.",
          "Tests assert every response includes status, result, attempts_used, attempts_remaining, feedback, and history.",
          "Tests assert no external dependencies are required."
        ],
        "interface_artifact_expectations": [
          "Runtime output keys and value types match the declared output_schema used by the tool result renderer.",
          "Human-readable result messages are concise and suitable for display as the primary tool result."
        ]
      },
      {
        "id": "wordle_user_docs",
        "title": "Skill README",
        "summary": "Write concise user and maintenance documentation for the Wordle Game skill, covering how the tool is used, its local-only behavior, cache state, permissions, and expected test command.",
        "depends_on": [
          "wordle_manifest_tool_contract",
          "wordle_gameplay_engine"
        ],
        "difficulty": "easy",
        "requires_tests": false,
        "parallel_safe": true,
        "expected_inputs": [
          "blueprint.json",
          "permissions.json",
          "manifest.json",
          "skill.py"
        ],
        "parent_interface_artifacts": [
          "tool_ui_schema"
        ],
        "expected_output_paths": [
          "README.md"
        ],
        "file_write_claims": [
          "README.md"
        ],
        "acceptance_criteria": [
          "README.md explains that the skill is a local Wordle-style automation tool with a six-attempt five-letter guessing flow.",
          "README.md documents the guess and new_game inputs and the status, result, attempts, feedback, and history outputs.",
          "README.md states that runtime state is stored only in ./cache and that no network, shell, secrets, or external dependencies are used.",
          "README.md does not claim the skill is installed, enabled, approved, or already run."
        ],
        "test_expectations": [],
        "interface_artifact_expectations": [
          "README.md describes the declarative Tools UI fields consistently with tool_ui_schema."
        ]
      }
    ],
    "edges": [
      {
        "from": "wordle_manifest_tool_contract",
        "to": "wordle_gameplay_engine"
      },
      {
        "from": "wordle_manifest_tool_contract",
        "to": "wordle_user_docs"
      },
      {
        "from": "wordle_gameplay_engine",
        "to": "wordle_user_docs"
      }
    ],
    "final_e2e_expectations": [
      "The generated skill contains manifest.json, skill.py, README.md, and tests/test_skill.py.",
      "Platform manifest validation passes.",
      "The test suite for executable behavior passes before install validation.",
      "The manifest permissions match the approved permission plan: no network, no secrets, no shell, no dependencies, and filesystem_write limited to ./cache.",
      "The tool can be rendered in the Tools UI from declarative tool_ui_schema without frontend code.",
      "The automation can play a complete local Wordle-style game through repeated calls with persisted history, valid feedback, and correct terminal states."
    ]
  },
  "interface_artifacts": [
    {
      "task_id": "wordle_manifest_tool_contract",
      "created_paths": [
        "manifest.json"
      ],
      "updated_paths": [],
      "interfaces": {
        "entrypoint": null,
        "input_schema": {
          "type": "object",
          "properties": {
            "guess": {
              "type": "string",
              "description": "A five-letter alphabetic Wordle guess.",
              "minLength": 5,
              "maxLength": 5,
              "pattern": "^[A-Za-z]{5}$"
            },
            "new_game": {
              "type": "boolean",
              "description": "Start a fresh game and clear the current cached game state.",
              "default": false
            }
          },
          "required": [
            "guess"
          ],
          "additionalProperties": false
        },
        "output_schema": {
          "type": "object",
          "properties": {
            "status": {
              "type": "string",
              "enum": [
                "in_progress",
                "won",
                "lost",
                "reset",
                "error"
              ]
            },
            "result": {
              "type": "string"
            },
            "attempts_used": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            },
            "attempts_remaining": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            },
            "feedback": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "letter": {
                    "type": "string"
                  },
                  "state": {
                    "type": "string",
                    "enum": [
                      "correct",
                      "present",
                      "absent"
                    ]
                  }
                },
                "required": [
                  "letter",
                  "state"
                ],
                "additionalProperties": false
              }
            },
            "history": {
              "type": "array",
              "items": {
                "type": "object"
              }
            }
          },
          "required": [
            "status",
            "result",
            "attempts_used",
            "attempts_remaining",
            "feedback",
            "history"
          ],
          "additionalProperties": false
        }
      },
      "contracts_for_children": [
        "The Tools page can render the tool from manifest.json using only tool_ui_schema.",
        "The guess field is required, text-based, labeled Guess, and includes a five-letter placeholder.",
        "The new_game field is optional, checkbox-based, labeled Start New Game, and defaults to false.",
        "The result rendering hint identifies result as the primary output field."
      ],
      "known_limitations": []
    },
    {
      "task_id": "wordle_gameplay_engine",
      "created_paths": [
        "skill.py"
      ],
      "updated_paths": [],
      "interfaces": {
        "entrypoint": "skill.py",
        "input_schema": {
          "type": "object",
          "properties": {
            "guess": {
              "type": "string",
              "description": "A five-letter alphabetic Wordle guess.",
              "minLength": 5,
              "maxLength": 5,
              "pattern": "^[A-Za-z]{5}$"
            },
            "new_game": {
              "type": "boolean",
              "description": "Start a fresh game and clear the current cached game state.",
              "default": false
            }
          },
          "required": [
            "guess"
          ],
          "additionalProperties": false
        },
        "output_schema": {
          "type": "object",
          "properties": {
            "status": {
              "type": "string",
              "enum": [
                "in_progress",
                "won",
                "lost",
                "reset",
                "error"
              ]
            },
            "result": {
              "type": "string"
            },
            "attempts_used": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            },
            "attempts_remaining": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            },
            "feedback": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "letter": {
                    "type": "string"
                  },
                  "state": {
                    "type": "string",
                    "enum": [
                      "correct",
                      "present",
                      "absent"
                    ]
                  }
                },
                "required": [
                  "letter",
                  "state"
                ],
                "additionalProperties": false
              }
            },
            "history": {
              "type": "array",
              "items": {
                "type": "object"
              }
            }
          },
          "required": [
            "status",
            "result",
            "attempts_used",
            "attempts_remaining",
            "feedback",
            "history"
          ],
          "additionalProperties": false
        }
      },
      "contracts_for_children": [
        "Runtime output keys and value types match the declared output_schema used by the tool result renderer.",
        "Human-readable result messages are concise and suitable for display as the primary tool result."
      ],
      "known_limitations": []
    },
    {
      "task_id": "wordle_user_docs",
      "created_paths": [
        "README.md"
      ],
      "updated_paths": [],
      "interfaces": {
        "entrypoint": "skill.py",
        "input_schema": {
          "type": "object",
          "properties": {
            "guess": {
              "type": "string",
              "description": "A five-letter alphabetic Wordle guess.",
              "minLength": 5,
              "maxLength": 5,
              "pattern": "^[A-Za-z]{5}$"
            },
            "new_game": {
              "type": "boolean",
              "description": "Start a fresh game and clear the current cached game state.",
              "default": false
            }
          },
          "required": [
            "guess"
          ],
          "additionalProperties": false
        },
        "output_schema": {
          "type": "object",
          "properties": {
            "status": {
              "type": "string",
              "enum": [
                "in_progress",
                "won",
                "lost",
                "reset",
                "error"
              ]
            },
            "result": {
              "type": "string"
            },
            "attempts_used": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            },
            "attempts_remaining": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            },
            "feedback": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "letter": {
                    "type": "string"
                  },
                  "state": {
                    "type": "string",
                    "enum": [
                      "correct",
                      "present",
                      "absent"
                    ]
                  }
                },
                "required": [
                  "letter",
                  "state"
                ],
                "additionalProperties": false
              }
            },
            "history": {
              "type": "array",
              "items": {
                "type": "object"
              }
            }
          },
          "required": [
            "status",
            "result",
            "attempts_used",
            "attempts_remaining",
            "feedback",
            "history"
          ],
          "additionalProperties": false
        }
      },
      "contracts_for_children": [
        "README.md describes the declarative Tools UI fields consistently with tool_ui_schema."
      ],
      "known_limitations": []
    }
  ],
  "task_id": "final_e2e",
  "test_file": "tests/test_final_e2e.py",
  "code_files": {
    "manifest.json": "{\n  \"name\": \"wordle_game\",\n  \"description\": \"Play a local six-attempt five-letter Wordle-style guessing game.\",\n  \"skill_type\": \"automation\",\n  \"interface_type\": \"tool\",\n  \"risk_level\": \"low\",\n  \"entrypoint\": \"skill.py\",\n  \"permissions\": {\n    \"network\": [],\n    \"filesystem_read\": [],\n    \"filesystem_write\": [\n      \"./cache\"\n    ],\n    \"secrets\": [],\n    \"shell\": false\n  },\n  \"created_by\": \"builder_agent\",\n  \"enabled\": false,\n  \"input_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"guess\": {\n        \"type\": \"string\",\n        \"description\": \"A five-letter alphabetic Wordle guess.\",\n        \"minLength\": 5,\n        \"maxLength\": 5,\n        \"pattern\": \"^[A-Za-z]{5}$\"\n      },\n      \"new_game\": {\n        \"type\": \"boolean\",\n        \"description\": \"Start a fresh game and clear the current cached game state.\",\n        \"default\": false\n      }\n    },\n    \"required\": [\n      \"guess\"\n    ],\n    \"additionalProperties\": false\n  },\n  \"output_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"status\": {\n        \"type\": \"string\",\n        \"enum\": [\n          \"in_progress\",\n          \"won\",\n          \"lost\",\n          \"reset\",\n          \"error\"\n        ]\n      },\n      \"result\": {\n        \"type\": \"string\"\n      },\n      \"attempts_used\": {\n        \"type\": \"integer\",\n        \"minimum\": 0,\n        \"maximum\": 6\n      },\n      \"attempts_remaining\": {\n        \"type\": \"integer\",\n        \"minimum\": 0,\n        \"maximum\": 6\n      },\n      \"feedback\": {\n        \"type\": \"array\",\n        \"items\": {\n          \"type\": \"object\",\n          \"properties\": {\n            \"letter\": {\n              \"type\": \"string\"\n            },\n            \"state\": {\n              \"type\": \"string\",\n              \"enum\": [\n                \"correct\",\n                \"present\",\n                \"absent\"\n              ]\n            }\n          },\n          \"required\": [\n            \"letter\",\n            \"state\"\n          ],\n          \"additionalProperties\": false\n        }\n      },\n      \"history\": {\n        \"type\": \"array\",\n        \"items\": {\n          \"type\": \"object\"\n        }\n      }\n    },\n    \"required\": [\n      \"status\",\n      \"result\",\n      \"attempts_used\",\n      \"attempts_remaining\",\n      \"feedback\",\n      \"history\"\n    ],\n    \"additionalProperties\": false\n  },\n  \"tool_ui_schema\": {\n    \"title\": \"Wordle Game\",\n    \"description\": \"Play a local six-attempt five-letter word guessing game.\",\n    \"submit_label\": \"Submit Guess\",\n    \"fields\": [\n      {\n        \"name\": \"guess\",\n        \"label\": \"Guess\",\n        \"type\": \"text\",\n        \"placeholder\": \"CRANE\",\n        \"help_text\": \"Enter exactly five letters.\",\n        \"required\": true,\n        \"default\": \"\"\n      },\n      {\n        \"name\": \"new_game\",\n        \"label\": \"Start New Game\",\n        \"type\": \"checkbox\",\n        \"placeholder\": \"\",\n        \"help_text\": \"Reset the current saved Wordle game before applying this guess.\",\n        \"required\": false,\n        \"default\": false\n      }\n    ],\n    \"result_template\": {\n      \"primary_field\": \"result\",\n      \"primary_label\": \"Result\"\n    }\n  }\n}\n",
    "README.md": "# Wordle Game\n\nWordle Game is a local Wordle-style automation tool. It runs a six-attempt guessing game where each guess must be exactly five alphabetic characters.\n\nThe tool is exposed through the declarative Tools UI described in `manifest.json`. The form contains:\n\n- `Guess`: required text input for a five-letter guess, with `CRANE` as the placeholder.\n- `Start New Game`: optional checkbox that clears the saved game state before applying the submitted guess.\n\n## Inputs\n\n- `guess`: A five-letter alphabetic word guess. Guesses are normalized to uppercase before feedback is produced.\n- `new_game`: When `true`, the current cached game is reset and the submitted guess starts a fresh game. Defaults to `false`.\n\n## Outputs\n\n- `status`: Current game status. Values are `in_progress`, `won`, `lost`, `reset`, or `error`.\n- `result`: Human-readable result message suitable for the primary tool result display.\n- `attempts_used`: Number of guesses used in the current game, from 0 to 6.\n- `attempts_remaining`: Number of guesses remaining in the current game, from 0 to 6.\n- `feedback`: Per-letter feedback for the submitted guess. Each item includes a `letter` and a `state` of `correct`, `present`, or `absent`.\n- `history`: Prior guesses and their feedback for the current game.\n\n## Runtime State\n\nGame state is stored only inside `./cache`. The cached state tracks the selected answer, terminal game status, and guess history so the next invocation can continue the same game.\n\nThe skill requests no network access, shell access, secrets, external dependencies, broad filesystem reads, or filesystem writes outside `./cache`.\n\n## Maintenance\n\nThe executable entrypoint is `skill.py`. It reads a JSON object from stdin, writes a JSON object to stdout, and avoids side effects on import.\n\nExpected validation includes manifest inspection and the TesterAgent-owned test command:\n\n```powershell\npython -m pytest tests/test_skill.py\n```\n\nThis proposed skill package does not claim that the skill is installed, enabled, approved, or already run.\n",
    "skill.py": "import json\nimport random\nimport sys\nfrom pathlib import Path\n\n\nMAX_ATTEMPTS = 6\nCACHE_DIR = Path(__file__).resolve().parent / \"cache\"\nSTATE_FILE_NAME = \"game_state.json\"\n\nANSWER_WORDS = [\n    \"CRANE\",\n    \"SLATE\",\n    \"PLANT\",\n    \"BRICK\",\n    \"CLOUD\",\n    \"SHINE\",\n    \"GRAPE\",\n    \"MIRTH\",\n    \"BLOOM\",\n    \"FROST\",\n    \"CHAIR\",\n    \"WATER\",\n    \"LIGHT\",\n    \"STONE\",\n    \"PRIDE\",\n    \"FLAME\",\n    \"SOUND\",\n    \"TRAIL\",\n    \"HONEY\",\n    \"SPICE\",\n]\n\n\ndef _state_file() -> Path:\n    return CACHE_DIR / STATE_FILE_NAME\n\n\ndef _empty_response(status: str, result: str) -> dict:\n    return {\n        \"status\": status,\n        \"result\": result,\n        \"attempts_used\": 0,\n        \"attempts_remaining\": MAX_ATTEMPTS,\n        \"feedback\": [],\n        \"history\": [],\n    }\n\n\ndef _error_response(message: str, state: dict | None = None) -> dict:\n    history = list(state.get(\"history\", [])) if isinstance(state, dict) else []\n    attempts_used = min(len(history), MAX_ATTEMPTS)\n    return {\n        \"status\": \"error\",\n        \"result\": message,\n        \"attempts_used\": attempts_used,\n        \"attempts_remaining\": max(MAX_ATTEMPTS - attempts_used, 0),\n        \"feedback\": [],\n        \"history\": history,\n    }\n\n\ndef _choose_answer() -> str:\n    return random.choice(ANSWER_WORDS)\n\n\ndef _new_state() -> dict:\n    return {\n        \"answer\": _choose_answer(),\n        \"history\": [],\n        \"terminal\": False,\n        \"status\": \"in_progress\",\n    }\n\n\ndef _load_state() -> dict | None:\n    path = _state_file()\n    if not path.exists():\n        return None\n\n    try:\n        with path.open(\"r\", encoding=\"utf-8\") as state_file:\n            state = json.load(state_file)\n    except (OSError, json.JSONDecodeError):\n        return None\n\n    if not isinstance(state, dict):\n        return None\n    answer = state.get(\"answer\")\n    history = state.get(\"history\")\n    if not isinstance(answer, str) or len(answer) != 5 or not answer.isalpha():\n        return None\n    if not isinstance(history, list):\n        return None\n\n    normalized_history = []\n    for item in history[:MAX_ATTEMPTS]:\n        if not isinstance(item, dict):\n            continue\n        guess = item.get(\"guess\")\n        feedback = item.get(\"feedback\")\n        if isinstance(guess, str) and isinstance(feedback, list):\n            normalized_history.append(\n                {\n                    \"guess\": guess.upper(),\n                    \"feedback\": feedback,\n                }\n            )\n\n    return {\n        \"answer\": answer.upper(),\n        \"history\": normalized_history,\n        \"terminal\": bool(state.get(\"terminal\", False)),\n        \"status\": state.get(\"status\", \"in_progress\"),\n    }\n\n\ndef _save_state(state: dict) -> None:\n    CACHE_DIR.mkdir(parents=True, exist_ok=True)\n    with _state_file().open(\"w\", encoding=\"utf-8\") as state_file:\n        json.dump(state, state_file, indent=2, sort_keys=True)\n\n\ndef _validate_guess(value: object) -> str | None:\n    if not isinstance(value, str):\n        return None\n    guess = value.strip().upper()\n    if len(guess) != 5 or not guess.isalpha():\n        return None\n    return guess\n\n\ndef _build_feedback(guess: str, answer: str) -> list[dict]:\n    states = [\"absent\"] * len(guess)\n    remaining_letters: dict[str, int] = {}\n\n    for index, letter in enumerate(guess):\n        if letter == answer[index]:\n            states[index] = \"correct\"\n        else:\n            answer_letter = answer[index]\n            remaining_letters[answer_letter] = remaining_letters.get(answer_letter, 0) + 1\n\n    for index, letter in enumerate(guess):\n        if states[index] == \"correct\":\n            continue\n        if remaining_letters.get(letter, 0) > 0:\n            states[index] = \"present\"\n            remaining_letters[letter] -= 1\n\n    return [\n        {\n            \"letter\": letter,\n            \"state\": states[index],\n        }\n        for index, letter in enumerate(guess)\n    ]\n\n\ndef _result_message(status: str, guess: str, attempts_used: int, answer: str) -> str:\n    if status == \"won\":\n        return f\"Correct: {guess}. You solved it in {attempts_used} attempt(s).\"\n    if status == \"lost\":\n        return f\"Game over. The answer was {answer}.\"\n    return f\"Guess accepted: {guess}. {MAX_ATTEMPTS - attempts_used} attempt(s) remaining.\"\n\n\ndef run(payload: dict) -> dict:\n    if not isinstance(payload, dict):\n        return _empty_response(\"error\", \"Input must be a JSON object.\")\n\n    state = _load_state()\n    if payload.get(\"new_game\") is True:\n        state = _new_state()\n    elif state is None or state.get(\"terminal\") is True:\n        state = _new_state()\n\n    guess = _validate_guess(payload.get(\"guess\"))\n    if guess is None:\n        return _error_response(\"Guess must be exactly five alphabetic characters.\", state)\n\n    answer = state[\"answer\"]\n    feedback = _build_feedback(guess, answer)\n    history_item = {\n        \"guess\": guess,\n        \"feedback\": feedback,\n    }\n    history = list(state.get(\"history\", []))\n    history.append(history_item)\n    attempts_used = min(len(history), MAX_ATTEMPTS)\n    attempts_remaining = max(MAX_ATTEMPTS - attempts_used, 0)\n\n    if guess == answer:\n        status = \"won\"\n        terminal = True\n    elif attempts_used >= MAX_ATTEMPTS:\n        status = \"lost\"\n        terminal = True\n    else:\n        status = \"in_progress\"\n        terminal = False\n\n    state.update(\n        {\n            \"history\": history,\n            \"terminal\": terminal,\n            \"status\": status,\n        }\n    )\n    _save_state(state)\n\n    return {\n        \"status\": status,\n        \"result\": _result_message(status, guess, attempts_used, answer),\n        \"attempts_used\": attempts_used,\n        \"attempts_remaining\": attempts_remaining,\n        \"feedback\": feedback,\n        \"history\": history,\n    }\n\n\ndef main() -> int:\n    try:\n        raw_input = sys.stdin.read()\n        payload = json.loads(raw_input) if raw_input.strip() else {}\n        response = run(payload)\n    except Exception as exc:\n        response = _empty_response(\"error\", f\"Unable to process input: {exc}\")\n\n    print(json.dumps(response, ensure_ascii=True))\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n"
  },
  "responsibility": "Tester writes one final end-to-end test and validates the complete proposed skill."
}